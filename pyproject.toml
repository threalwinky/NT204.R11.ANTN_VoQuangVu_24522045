[project]
name = "main"
version = "0.1.0"
description = "Build my first IDS"
readme = "README.md"
authors = [
    { name = "threalwinky" }
]
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.1.1",
    "scapy>=2.7.0",
]

[project.scripts]
main = "main:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
